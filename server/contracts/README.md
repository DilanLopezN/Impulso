# `contracts/`

Contratos compartilhados entre clientes (mobile/web) e back-end:

- `openapi.yaml` — contrato HTTP da API (a ser publicado).
- `events/` — schemas dos eventos de domínio publicados na fila.

Ainda vazio. Será preenchido conforme os módulos da API forem implementados.
